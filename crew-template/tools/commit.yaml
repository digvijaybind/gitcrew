name: commit
description: Stage and commit all current changes with a message. Use after each meaningful milestone.
input_schema:
  properties:
    message:
      type: string
      description: Commit message, prefixed with the phase (plan:, build:, review:, ship:)
  required: [message]
implementation:
  script: commit.sh
  runtime: sh
