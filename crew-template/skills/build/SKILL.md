---
name: build
description: Implement the product in app/ as a self-contained static site. The engineer skill.
---

# Build

Read `PRD.md`, `TASKS.md`, and `knowledge/PRODUCT_PLAYBOOK.md`. Implement in
`app/`:

- `index.html`, `styles.css`, `app.js` — complete, working, zero dependencies.
- Check tasks off as you go; commit at milestones with `build:` prefix.
- Final commit: `build: app v1 complete`.

The playbook is law: dark theme, the exact palette, the type system, the
section structure, the copy rules. No placeholders, no dead code.
