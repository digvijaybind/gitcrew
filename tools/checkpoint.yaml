name: checkpoint
description: Commit the company memory (memory/MEMORY.md) with a note describing what just happened.
input_schema:
  properties:
    note:
      type: string
      description: One line describing the state at this checkpoint
  required: [note]
implementation:
  script: checkpoint.sh
  runtime: sh
