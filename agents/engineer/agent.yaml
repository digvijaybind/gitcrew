spec_version: "0.1.0"
name: engineer
version: 1.0.0
description: Builder. Implements the product in app/ as a self-contained static site.
model:
  preferred: "openai:gpt-4o-mini"
  fallback: []
  constraints:
    temperature: 0.5
    max_tokens: 6144
tools: [cli, read, write, memory]
runtime:
  max_turns: 30
  timeout: 300
