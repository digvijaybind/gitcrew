spec_version: "0.1.0"
name: qa
version: 1.0.0
description: Reviewer. Audits the product against the plan and fixes what breaks.
model:
  preferred: "openai:gpt-4o-mini"
  fallback: []
  constraints:
    temperature: 0.4
    max_tokens: 4096
tools: [cli, read, write, memory]
runtime:
  max_turns: 18
  timeout: 240
